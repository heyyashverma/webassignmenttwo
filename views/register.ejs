<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Register</title>
  <link href="/css/main.css" rel="stylesheet" />
</head>
<body class="bg-base-200 min-h-screen">
  <%- include('partials/navbar') %>
  <main class="flex flex-col items-center justify-center min-h-[80vh] px-2">
    <div class="w-full max-w-2xl bg-gray-800 rounded-xl p-8 mt-8">
      <h2 class="text-5xl font-bold text-center mb-2 text-gray-200">Register</h2>
      <p class="text-center text-gray-400 mb-8">Create your account below</p>
      <% if(!successMessage){ %>
      <form method="POST" action="/register" class="space-y-6">
        <% if(errorMessage){ %>
        <div class="font-bold text-center mb-4">
          <%= errorMessage %>
        </div>
        <% } %>
        <div class="mb-4">
          <label class="block mb-1 text-gray-400" for="userName">User Name</label>
          <input type="text" id="userName" name="userName" placeholder="User Name" value="<%= userName %>" required class="input input-bordered w-full bg-base-200 text-gray-100" />
        </div>
        <div class="mb-4">
          <label class="block mb-1 text-gray-400" for="password">Password</label>
          <input type="password" id="password" name="password" placeholder="Password" required class="input input-bordered w-full bg-base-200 text-gray-100" />
        </div>
        <div class="mb-4">
          <label class="block mb-1 text-gray-400" for="password2">Confirm Password</label>
          <input type="password" id="password2" name="password2" placeholder="Confirm Password" required class="input input-bordered w-full bg-base-200 text-gray-100" />
        </div>
        <div class="mb-4">
          <label class="block mb-1 text-gray-400" for="email">Email Address</label>
          <input type="email" id="email" name="email" placeholder="Email Address" required class="input input-bordered w-full bg-base-200 text-gray-100" />
        </div>
        <button type="submit" class="btn btn-success w-fit px-8 mb-2">Register</button>
      </form>
      <div class="mt-6 text-center text-sm text-gray-400">
        Already have an account? <a href="/login" class="text-success font-semibold hover:underline">Login</a>
      </div>
      <% } else { %>
      <div class="alert mt-4">
        <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
        <span><%= successMessage %></span>
      </div><br />
      <a href="/login" class="btn btn-success w-full">Proceed to Log in</a>
      <% } %>
    </div>
  </main>
</body>
</html>
